# Основні бібліотеки
aiogram==3.2.0
asyncpg==1.37.0
beautifulsoup4==4.12.2
newspaper3k==0.2.8
lxml==4.9.3  # Wheel, щоб не збиралося з джерел
requests==2.32.0
aiohttp==3.9.3
python-dotenv==1.0.0

# Для перекладу офлайн (якщо використовується)
translate==4.5.0

# Опціонально для роботи з JSON, datetime
orjson==3.9.1
python-dateutil==2.8.2

# Для локального NLP / AI аналізу (якщо потрібен)
transformers==4.41.0
torch==2.2.0

# Примусово завантажує готові колеса, щоб уникнути збірки
--prefer-binary
